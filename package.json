{
  "name": "ai-webcam-effects-extension",
  "version": "1.0.0",
  "description": "Browser extension for real-time AI webcam video enhancement and beautification",
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode production --watch",
    "build:dev": "webpack --mode production",
    "benchmark:serve": "python3 -m http.server 4173",
    "benchmark:download-samples": "node scripts/download-acne-samples.js"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.14"
  }
}
